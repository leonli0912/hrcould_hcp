<mvc:View controllerName="ui5TileTrial.controller.EmployeeList"
	xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc">

<!-- 	<Page id="idEmployeeList" title="Employees" icon="sap-icon://action"
		showNavButton="true" navButtonPress="onPressHome" class="sapUiStdPage">
		<headerContent>
			<Button icon="sap-icon://add" press="onPressAdd">
			</Button>
		</headerContent>
		<content> -->
		 	<List id="id_EmployeeList" items="{/}" enableBusyIndicator="true">
				<StandardListItem 
				title="{title}"
					info="{userId}"
					description="{username}" 
					type="Navigation" 
					icon="{ProductPicUrl}"
					iconDensityAware="false" 
					iconInset="false" 
					press="onPressToDetail" />
			</List>
<!-- 		</content>

	</Page> -->
</mvc:View>